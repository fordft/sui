[package]
name = "sui"
version = "0.4.3"
edition = "2021"
description = "cache-first multi-agent coding harness"
repository = "https://github.com/fordft/sui"
homepage = "https://github.com/fordft/sui"

[dependencies]
agent-client-protocol = "=2.1.0"
anyhow = "1"
async-process = "2"
base64 = "0.22"
clap = { version = "4", features = ["derive"] }
crossterm = { version = "0.28", features = ["event-stream", "bracketed-paste"] }
futures-util = { version = "0.3", features = ["io"] }
keyring = { version = "3", default-features = false, features = ["apple-native", "windows-native", "linux-native"] }
rand = "0.10.2"
ratatui = "0.29"
regex = "1.13.1"
reqwest = { version = "0.13", default-features = false, features = ["json", "stream", "form", "rustls"] }
rmcp = { version = "3.3.0", default-features = false, features = ["client", "transport-streamable-http-client-reqwest"] }
rustix = { version = "1", features = ["process"] }
rustyline = "15"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10.9"
tokio = { version = "1", features = ["full"] }
toml = "0.8"
unicode-segmentation = "1"
unicode-width = "=0.2.0"

[dev-dependencies]
portable-pty = "0.8"

[[bin]]
name = "sui-certify"
path = "src/bin/certify.rs"

[[bin]]
name = "sui-mission"
path = "src/bin/mission.rs"

[workspace]

[profile.release]
lto = true
codegen-units = 1

# cargo-dist builds use this profile (inherits release)
[profile.dist]
inherits = "release"

# cargo-dist release pipeline: tag v* → CI build → GitHub Release assets
# (archives + checksums + sui-installer.sh). Private install dir keeps us
# off other `sui` binaries (e.g. the Mysten toolchain).
[workspace.metadata.dist]
cargo-dist-version = "0.33.0"
ci = ["github"]
installers = ["shell", "homebrew"]
# One tap, one formula: brew install fordft/tap/sui-ai (macOS + Linux)
tap = "fordft/homebrew-tap"
formula = "sui-ai"
publish-jobs = ["homebrew"]
targets = [
    "aarch64-apple-darwin",
    "x86_64-apple-darwin",
    "x86_64-unknown-linux-gnu",
    "aarch64-unknown-linux-gnu",
]
install-path = "$HOME/.sui/bin"
github-attestations = true
pr-run-mode = "plan"

# Thin guarded wrapper: collision check vs other `sui` commands, then
# delegates to the generated sui-installer.sh. Published as install.sh.
[[workspace.metadata.dist.extra-artifacts]]
artifacts = ["install.sh"]
build = ["true"]
